<h3><img src="http://i.imgur.com/LP2gCs5.jpg" title="source: imgur.com" /></h3>

<p><img src="http://i.imgur.com/pgVIUsW.gif" title="source: imgur.com" /></p>

<h2>all tasks</h2>
<div class="tasks sortable" id="all_tasks">
  <%= render @tasks %>
</div>

<div class ="empty"></div>

<div id="new_link">
  <%= link_to "create new task", new_task_path, id: "new_link", remote: true , class: 'btn-create' %>
</div>




















<!-- <p id="notice"><%= notice %></p>

<h1>Listing Tasks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Priority</th>
      <th>Complete</th>
      <th>Due date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.description %></td>
        <td><%= task.priority %></td>
        <td><%= task.complete %></td>
        <td><%= task.due_date %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %> -->
