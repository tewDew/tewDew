<h1>tewDew</h1>

<form class="form-inline" role="form">
<h2>Incomplete Tasks</h2>
<div class="tasks sortable" id="incomplete_tasks">
  <%= render @tasks %>
</div>

<h2>Complete Tasks</h2>
<div class="tasks sortable" id="complete_tasks">
  <%= render @tasks %>
</div>
</form>

<div id="new_link">
  <p class="task-muted"><%= link_to "Create Task", new_task_path, id: "new_link", remote: true %></p>
</div>




















<!-- <p id="notice"><%= notice %></p>

<h1>Listing Tasks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Priority</th>
      <th>Complete</th>
      <th>Due date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.description %></td>
        <td><%= task.priority %></td>
        <td><%= task.complete %></td>
        <td><%= task.due_date %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %> -->
